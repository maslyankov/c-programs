/*3) За даден
"typedef char (*filter_t)(char);"
 да се напише функция
void apply_filter(char *, filter_t);
 която прилага функцията filter_t
 над всеки символ зад първия аргумент до '\0'.
*/

typedef char (*filter_t)(char);

void apply_filter(char* string, filter_t p){

}

int main(int argc, char const *argv[]) {
        /* code */
        return 0;
}
